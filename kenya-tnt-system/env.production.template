# ============================================
# PRODUCTION ENVIRONMENT (Company Server)
# ============================================
# Used with: docker compose -f docker-compose.production.yml up -d
# Server: TBD (Your company's production server)

# Database
POSTGRES_DB=kenya_tnt_production
POSTGRES_USER=tnt_prod_user
POSTGRES_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD
POSTGRES_PORT=5432

# Backend
NODE_ENV=production
LOG_LEVEL=warn
DB_HOST=kenya-tnt-postgres
DB_PORT=5432
DB_USERNAME=tnt_prod_user
DB_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD
DB_DATABASE=kenya_tnt_production
PORT=4000
JWT_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET
JWT_EXPIRES_IN=7d
CORS_ORIGIN=https://yourdomain.com,https://app.yourdomain.com
ENABLE_DETAILED_ERRORS=false

# Frontend
NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com/api

# Optional: External services
# KAFKA_BROKERS=kafka:9092
# OPENSEARCH_NODE=http://opensearch-node:9200
# EPCIS_SERVICE_URL=http://epcis-service:8080

# Production-specific
# SENTRY_DSN=your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-newrelic-key

